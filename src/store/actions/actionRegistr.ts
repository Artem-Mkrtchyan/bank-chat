import { authAPI } from '../../axios/authAPI'
import { authSlice } from '../slices/authSlice'
import { AppDispatch } from '../store'

export const registration = () => async (dispatch: AppDispatch) => {


}

